image: "python:3.7"

before_script:
  - apt-get update -qy
  - apt-get install -y python-dev python-pip
  - python --version
  - pip install -r requirements.txt

stages:
  - test

test:
  stage: test
  script:
  - pytest